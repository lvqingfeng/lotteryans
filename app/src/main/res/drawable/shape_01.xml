<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval"
    android:useLevel="false">
    <!-- shape可以绘制矩形环形以及椭圆，
        所以只需要用椭圆即可，在使用的时候将控件
        比如imageview或textview的高宽设置成一样就是正圆
    -->
    <!-- solid表示圆的填充色 -->
    <solid
        android:color="@color/white"
        />
    <!-- stroke则代表圆的边框线
   	 所以两者结合可以实现带边缘的圆，
   	 当然也可以直接加上size控制高宽
    -->
    <stroke
        android:width="1dp"
        android:color="@color/colorAccent"/>
    <!--
    <size
        android:width="20dp"
        android:height="20dp"/>
        -->
</shape>